# Local-dev defaults. Copy to .env and adjust.
MONGO_URL=mongodb://localhost:27017
DB_NAME=aetherion
CORS_ORIGINS=http://localhost:3000
# Used ONLY by the offline texture tool backend/tools/gen_textures.py, never by the running server.
EMERGENT_LLM_KEY=
